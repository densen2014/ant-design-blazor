@layout BlankLayout
@page "/"


<p>
    <Tree TItem="string" Checkable
          @bind-CheckedKeys="_checkedKeys">
        <TreeNode Title="综合管理" Key="01" TItem="string">
            @for (int i = 7; i < 10; i++)
            {
                <TreeNode Title="@($"刷脸信息管理{i}")" Key="@($"010{i}")" TItem="string"></TreeNode>
            }
        </TreeNode>
    </Tree>
</p>
<p>

</p>

<p>
    <Button @onclick="@OnSave">
        保存
    </Button>

</p>

@code
{
    string[] _checkedKeys = new string[] { "0901" };
    protected override void OnInitialized()
    {
        List<string> list = new List<string>();
        @for (int i = 0; i < 5; i++)
        {
            list.Add($"010{i}");
        }
        _checkedKeys = list.ToArray();
    }
    public void OnSave()
    {
    }
}